# 完整数据生成管线

状态：后续集成实现的预留位置，当前尚无完整管线代码。

当前与后续各阶段需要的实现将在集成时整理至本目录。完整程序运行须独立于 `data_generation/stage` 中的源码和启动程序，也不得在运行时复制或动态加载阶段代码。此目录名称不表示完整方法、LEM 联调或终态验证已经完成。

对应的软件测试将放在根目录 `tests/data_generation/pipeline`。
