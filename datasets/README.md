# 训练数据集

本目录位于项目根目录，与 `references` 平级，专用于供模型训练使用的数据集。

当前尚未在本目录放入训练样本。原来保存在此处的参考资料已按用途迁入 [references](../references/README.md)，来源路径、对应位置和 SHA-256 见 [参考资料索引](../references/catalog.csv)。

训练数据文件和目录按 `.gitignore` 排除。数据生成过程中的阶段产物、参考观测和实验结果分别保留各自用途，进入训练集时需要按训练任务的数据约定整理。
